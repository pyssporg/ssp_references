# IMP-016: Update Fixture Mapping and Documentation for signal_nested_parameter_bindings

## Status

Proposed

## Layer

Documentation (04-verification, models) — update `co_simulation_fixture_mapping.md`

## Theme

Add the `signal_nested_parameter_bindings` fixture to the fixture mapping
document, packaging coverage table, and ensure documentation completeness.

## Evidence

- `signal_nested_parameter_bindings` exists at `models/ssp/signal_nested_parameter_bindings/`
  with `FIXTURE.md`, `build.py`, and checked-in resources.
- It is registered in `artifacts/simulation_registry.json` and listed in
  `models/README.md`.
- It is NOT listed in `product-breakdown/04-verification/co_simulation_fixture_mapping.md`
  under the "Implemented Custom Signal-Propagation Fixtures" table.
- It is NOT listed in the packaging coverage table in
  `co_simulation_fixture_mapping.md`.
- The existing backlog acknowledges this gap.

## Current Pain Or Risk

- Documentation gap: the fixture exists but isn't tracked in the strategy
  document that maps fixtures to coverage.
- Developers reviewing the fixture mapping will miss a significant fixture
  that exercises mixed parameter binding scopes.
- The fixture's role in parameter precedence testing is not visible from the
  strategy document.

## Proposed Improvement

1. Add `signal_nested_parameter_bindings` to the "Implemented Custom
   Signal-Propagation Fixtures" table in `co_simulation_fixture_mapping.md`.
2. Add it to the packaging coverage discussion in the same document
   (external root SSV+SSM with inline nested-system bindings).
3. Update the packaging coverage table to explicitly list each fixture's
   packaging pattern.

## Expected Benefit

- Complete documentation of all existing fixtures in the strategy document.
- Developers can see the full scope of parameter packaging coverage.
- No orphan fixture entries.

## Risk And Blast Radius

Low. Documentation-only change. No code modification required.

## Suggested Priority

Low

## Task Contract Seed

1. Edit `product-breakdown/04-verification/co_simulation_fixture_mapping.md`.
2. Add to the "Implemented Custom Signal-Propagation Fixtures" table:
   - Fixture: `signal_nested_parameter_bindings`
   - Status: Implemented
   - Detail: link to FIXTURE.md
3. Add to the packaging coverage section: "external root SSV+SSM, inline
   nested-system bindings, component-scoped inline bindings" as a distinct
   pattern.
4. Ensure the FIXTURE.md's packaging notes accurately describe the pattern.

## Out Of Scope

- Modifying the fixture or its build script.
- Adding new test cases (other candidates cover testing).
- Changing the registry or any pipeline code.

## Traceability

- Verification: `co_simulation_fixture_mapping.md` — the document that provides
  fixture-to-strategy mapping.
- Models: `models/ssp/signal_nested_parameter_bindings/` — the fixture itself.
